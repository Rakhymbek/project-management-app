export enum EStorage {
  language = 'language',
  token = 'userToken',
}

export enum ELang {
  en = 'en',
  ru = 'ru',
}

export enum EDialogEvents {
  create = 'create',
  cancel = 'cancel',
  delete = 'delete',
  edit = 'edit',
}

export enum BoardElements {
  board = 'board',
  column = 'column',
  task = 'task',
}
